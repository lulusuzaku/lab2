const sum = require('./sum');

test('adds 1 + 2 to equal 3', () => {
  expect(sum(1, 2)).toBe(3);
});
// ставь npm install --save-dev jest-fetch-mock
// это только ты можешь ) для безопасности) но я могу смотреть =)
// поставилось ) да)
//снова эта твоя штука)